# goltfisch:braintree-server
This package adds the braintree NPM-Package to your Meteor project.

## Installation
Run `meteor add goltfisch:braintree-server` and you can use the `braintree` variable on the server-Side.
